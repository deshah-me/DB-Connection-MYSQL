The command is :
1. mysqld --initialize-insecure
2. mysqld --console
run this command in new cmd
3. mysql -u root --skip-password

Application.yml

spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydb
    username:  root
    password:
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
